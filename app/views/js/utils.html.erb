<header class="page">
  <div class="patterns"></div>
  <div class="container content">
    <div class="row">
      <div class="span10 offset1">
        <h4>Js - Utils</h4>
      </div>
    </div>
  </div>
</header>
<div class="container container-box">
  <div class="row">
    <div class="span10 offset1">
      <div class="box">
        <p>The object "Utils", contains some general methods of aids for classes of Lol. Besides storage of reference of
          instantiation. Below you can check the best content.</p>

        <hr />
        <h4 id="methods">Methods</h4>
        <p></p>

        <pre class="prettyprint lang-js linenums"><%= %`Lol.Utils = {
  /*
  Generates a unique string for script execution.

  original by: Kevin van Zonneveld (http://kevin.vanzonneveld.net)
  revised by: Kankrelune (http://www.webfaktory.info/)
  @return {String}
  */
  uniqid: function(prefix, more_entropy) {},
  /*
  Convert to string
  @return {String}
  */
  toString: function(obj) {},
  /*
  Adds an object to the collection Lol.
  */
  addObject: function(obj) {},
  /*
  Retrieves an object from the collection.
  */
  getObject: function(id) {},
  /*
  Removes an object from the collection.
  */
  removeObject: function(id) {},
  /*
  Redirects the page to another url.
  */
  redirector: function(url) {}
};` %></pre>

        <blockquote>
          <strong>uniqid</strong> ( Receive: <code>String -> prefix</code>, <code>Boolean -> more_entropy</code>
          | Returns: <code>String</code> )
          <p>Generates a unique string for script execution.</p>
          <blockquote>
            <pre class="prettyprint linenums lang-js"><%= %`Lol.Utils.uniqid();            // a30285b160c14
Lol.Utils.uniqid('foo');       // fooa30285b1cd361
Lol.Utils.uniqid('bar', true); // bara20285b23dfd1.31879087` %></pre>
          </blockquote>
        </blockquote>

        <blockquote>
          <strong>toString</strong> ( Receive: <code>Mixin</code> | Returns: <code>String</code> )
          <p>Convert to string.</p>
          <blockquote>
            <pre class="prettyprint linenums lang-js"><%= %`var obj;
obj = 123123123;
obj = Lol.Utils.toString(obj); // "123123123"` %></pre>
          </blockquote>
        </blockquote>

        <blockquote>
          <strong>addObject</strong> ( Receive: <code>Object</code> | Returns: <code>Object</code> )
          <p>Adds an object to the collection Lol.</p>
          <blockquote>
            <pre class="prettyprint linenums lang-js"><%= %`var obj;
obj = {
  id: Lol.Utils.uniqid()
};
Lol.Utils.addObject(obj);` %></pre>
          </blockquote>
        </blockquote>

        <blockquote>
          <strong>getObject</strong> ( Receive: <code>Type</code> Returns: <code>Type</code> )
          <p>Retrieves an object from the collection.</p>
          <blockquote>
            <pre class="prettyprint linenums lang-js"><%= %`var obj;
obj = {
  id: Lol.Utils.uniqid()
};
Lol.Utils.addObject(obj);
Lol.Utils.getObject(obj.id); // obj` %></pre>
          </blockquote>
        </blockquote>

        <blockquote>
          <strong>removeObject</strong> ( Receive: <code>Type</code> Returns: <code>Type</code> )
          <p>Removes an object from the collection.</p>
          <blockquote>
            <pre class="prettyprint linenums lang-js"><%= %`var obj;
obj = {
  id: Lol.Utils.uniqid()
};
Lol.Utils.addObject(obj);
Lol.Utils.removeObject(obj.id); // true` %></pre>
          </blockquote>
        </blockquote>

        <blockquote>
          <strong>redirector</strong> ( Receive: <code>Type</code> Returns: <code>Type</code> )
          <p>Redirects the page to another url.</p>
          <blockquote>
            <pre class="prettyprint linenums lang-js"><%= %`Lol.Utils.redirector("http://www.zaez.net/");` %></pre>
          </blockquote>
        </blockquote>
      </div>
    </div>
  </div>
</div>